# Registry padrão do NPM
registry=https://registry.npmjs.org/

# Configurações de publicação
access=public

# Sempre usar exact versions
save-exact=true

# Configuração para packages scoped (se você quiser usar @seu-username/lumos-ui)
# @seu-username:registry=https://registry.npmjs.org/

# Configurações de segurança
audit-level=moderate

# Engine strict (para garantir compatibilidade de Node)
engine-strict=true